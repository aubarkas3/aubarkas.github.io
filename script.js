function buyTicket(platform) {
    alert('Redirecting to ' + platform + '...');
}
function handleSearch(event) {
    event.preventDefault(); // Prevents the form from submitting
    alert('Searching for your event');
}
